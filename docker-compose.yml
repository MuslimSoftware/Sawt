services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - ./backend/.env
    environment:
      - WHISPER_MODEL=ggml-base.en.bin
    networks:
      - app-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      - NEXT_PUBLIC_BACKEND_WS=wss://sawt-api.younesbenketira.com/ws
    depends_on:
      - backend
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
